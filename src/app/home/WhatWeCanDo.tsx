import SectionHeader from "./SectionHeader";

export default function WhatWeCanDo() {
  return (
    <div className="flex flex-col items-center justify-center relative">
      <SectionHeader text="what we can do" className="mt-18 w-full px-6" />
    </div>
  );
}
